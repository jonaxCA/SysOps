{
  "name": "sysops-fork-backend",
  "version": "1.0.0",
  "description": "Backend Node con fork() real para el simulador SO. Cada proceso simulado se materializa como un proceso hijo del sistema operativo (PID real).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.16.0"
  }
}
